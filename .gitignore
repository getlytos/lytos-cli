# Dependencies
node_modules/

# Build
dist/

# Environment
.env
.env.local

# IDE
.vscode/
.idea/
*.code-workspace

# Claude Code
.claude/

# OS
.DS_Store
Thumbs.db

# Test coverage
coverage/

# npm
*.tgz

# Lytos: BOARD.md is a derived artifact, regenerated by `lyt board` from the
# issue frontmatter (the source of truth). Tracking it causes recurring merge
# conflicts on multi-change PRs. See .lytos/adr/ADR-0002-board-md-derived-artifact.md
.lytos/issue-board/BOARD.md

# Lytos: local, transient AI-wrapper session journal (ADR-0003). Absorbed into
# issue frontmatter via `lyt absorb`; never committed.
.lytos/.runtime/
