---
lockfile: package-lock.json
docs_source: vendored
---

# Stack contract (lytos-cli)

*TypeScript / Node 20+ CLI. The lockfile is truth for pinned versions. Runtime deps are
kept to the bone (manifest: "zero runtime dependencies beyond Commander.js"); anything
outside the list below needs an ADR, not a silent add.*

## Allowed dependencies

- commander
